declare const config: {
    input: string;
    output: {
        esModule: boolean;
        file: string;
        format: string;
        sourcemap: boolean;
    };
    plugins: any[];
};
export default config;
